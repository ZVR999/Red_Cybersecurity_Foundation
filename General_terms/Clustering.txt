This allows failover capabilities for servers by using multiple servers together.

These servers would be providing the same service which allows for better availability in case one decides to peace out.